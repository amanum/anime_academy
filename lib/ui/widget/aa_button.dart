import 'package:flutter/material.dart';

class AaButton extends StatelessWidget {
  const AaButton._({super.key});

  factory AaButton.primary() => const AaButton._();

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
